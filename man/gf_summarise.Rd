% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gf_summarise.R, R/todo.R
\name{gf_summarise}
\alias{gf_summarise}
\title{Compute diagnostic summary tables from a gf_result}
\usage{
gf_summarise(
  gf_result,
  thr_frac_good = 0.2,
  thr_frac_bad = 0.4,
  thr_cor_good = 0.6,
  thr_cor_bad = 0.4,
  filter_frac_peak = 0.3,
  filter_cor = 0.5
)

gf_summarise(
  gf_result,
  thr_frac_good = 0.2,
  thr_frac_bad = 0.4,
  thr_cor_good = 0.6,
  thr_cor_bad = 0.4,
  filter_frac_peak = 0.3,
  filter_cor = 0.5
)
}
\arguments{
\item{gf_result}{An object of class \code{gf_result} produced by
\code{\link{turnover_curves}}.}

\item{thr_frac_good}{numeric. Concentration threshold below which a SNP
is considered well-distributed. Default 0.2.}

\item{thr_frac_bad}{numeric. Concentration threshold above which a SNP
is considered potentially overfit. Default 0.4.}

\item{thr_cor_good}{numeric. Correlation-to-mean-curve threshold above
which a SNP is considered stable. Default 0.6.}

\item{thr_cor_bad}{numeric. Correlation threshold below which a SNP
is considered unstable. Default 0.4.}

\item{filter_frac_peak}{numeric. Hard filter: SNPs with frac_peak above
this are excluded from \code{snps_keep}. Default 0.3.}

\item{filter_cor}{numeric. Hard filter: SNPs with cor_to_mean below this
are excluded from \code{snps_keep}. Default 0.5.}
}
\value{
An object of class \code{gf_summary}.

An object of class \code{gf_summary}.
}
\description{
Produces per-SNP and per-variable summaries, turnover concentration
metrics, curve stability (correlation to mean), and zone classification
(good / borderline / overfit).

Produces per-SNP and per-variable summaries, turnover concentration
metrics, curve stability (correlation to mean), and zone classification
(good / borderline / overfit).
}
